{
  "name": "county-connect",
  "private": true,
  "version": "2.0.0",
  "description": "County Connect - Navarro County community platform",
  "workspaces": [
    "frontend",
    "strapi"
  ],
  "scripts": {
    "dev": "echo 'Run: cd frontend && npm run dev' && echo 'Run: cd strapi && npm run develop'",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:strapi": "cd strapi && npm run develop"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
