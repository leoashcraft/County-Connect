'use client';

// Client-side interactive components for service pages
// (e.g., phone call buttons, inquiry form triggers)
// Currently a placeholder - add interactive elements as needed

export function ServicePageClient({ page }: { page: any }) {
  return null;
}
