# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=7d

# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Database Configuration
# =====================
# Option 1: Use DATABASE_URL (Railway/Heroku style - RECOMMENDED for production)
# If DATABASE_URL is set, it takes precedence over individual variables
# Format: mysql://user:password@host:port/database_name
# DATABASE_URL=mysql://user:password@host:port/county_connect

# Option 2: Use individual variables
# Database type: 'sqlite' or 'mysql'
DATABASE_TYPE=sqlite

# SQLite settings (used when DATABASE_TYPE=sqlite, good for local development)
DATABASE_PATH=./database.sqlite

# MySQL settings (used when DATABASE_TYPE=mysql)
DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_USER=root
DATABASE_PASSWORD=your_database_password
DATABASE_NAME=county_connect

# CORS Configuration
FRONTEND_URL=http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Umami Analytics API (for admin dashboard)
# Get your API key from Umami Cloud: Settings → API Keys
UMAMI_API_URL=https://api.umami.is
UMAMI_API_KEY=your_umami_api_key_here
# Note: VITE_UMAMI_WEBSITE_ID should be set in the frontend .env
